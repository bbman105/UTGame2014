using UnityEngine;
using System.Collections;

public class test : MonoBehaviour
{

    void OnClick()
    {
        /*
        GameObject[] chara = GameObject.FindGameObjectsWithTag("RoomChara");
        if (caseID == 0)
        {
            chara[0].GetComponent<PlayerMonster>().CheckEvolution();
            caseID = 1;
        }
        else
        {
            chara[0].GetComponent<RoomCharaStatus>().Action(4, 0);
            caseID = 0;
        }

        */
    }

    // Use this for initialization
    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
